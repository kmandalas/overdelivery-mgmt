spring:
  application:
    name: spend-aggregator-test
  cloud:
    bus:
      enabled: false
    config:
      enabled: false
      discovery:
        enabled: false
eureka:
  client:
    enabled: false

